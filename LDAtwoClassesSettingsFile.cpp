//LDAtwoClasses settings file
//raw data: each each line should be one training example
// each column one feature. The last column should contain the label
// To be changed later: we have two classes labeled either 0 or 1
// Also to be changed: only works for two features
// To be added later: QDA or LDA?

std::string data = "irisDataRevised";
int numExp = 100;
int numFeat = 4;
//bool QDA == False //True or False
int plotFeat1 = 0;
int plotFeat2 = 1;
